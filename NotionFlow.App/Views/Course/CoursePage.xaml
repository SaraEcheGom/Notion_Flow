<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NotionFlow.App.Views.Course.CoursePage"
             Title="{Binding CourseName}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Label Text="{Binding CourseName}"
                   FontSize="{StaticResource FontSizeXL}" FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <!-- Botón + solo para profesor -->
            <Button Text="+"
                    FontSize="{StaticResource FontSizeXL}"
                    Command="{Binding ShowOptionsCommand}"
                    IsVisible="{Binding IsTeacher}"
                    HorizontalOptions="End"
                    WidthRequest="50"
                    HeightRequest="50"/>

            <!-- Evaluaciones -->
            <Label Text="Evaluaciones" FontSize="{StaticResource FontSizeLG}" FontAttributes="Bold"/>

            <CollectionView ItemsSource="{Binding Evaluations}"
                            EmptyView="Sin evaluaciones.">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,{StaticResource SpacingXS}" Padding="{StaticResource SpacingSM}"
                                StrokeThickness="1" Stroke="{StaticResource Gray200}"
                                StrokeShape="RoundRectangle {StaticResource RadiusSM}">
                            <VerticalStackLayout>
                                <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                <Label Text="{Binding Description}" FontSize="{StaticResource FontSizeXS}" TextColor="{StaticResource TextMuted}"/>
                                <Label Text="{Binding PercentageValue, StringFormat='Valor: {0}%'}" FontSize="{StaticResource FontSizeXS}"/>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Contenidos -->
            <Label Text="Contenidos" FontSize="{StaticResource FontSizeLG}" FontAttributes="Bold"/>

            <CollectionView ItemsSource="{Binding Contents}"
                            EmptyView="Sin contenidos.">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,{StaticResource SpacingXS}" Padding="{StaticResource SpacingSM}"
                                StrokeThickness="1" Stroke="{StaticResource Gray200}"
                                StrokeShape="RoundRectangle {StaticResource RadiusSM}">
                            <VerticalStackLayout>
                                <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                <Label Text="{Binding Description}" FontSize="{StaticResource FontSizeXS}" TextColor="{StaticResource TextMuted}"/>
                                <Label Text="{Binding Type}" FontSize="{StaticResource FontSizeXXS}" TextColor="{StaticResource PrimaryDark}"/>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Actividades -->
            <Label Text="Actividades (Cuestionarios)" FontSize="{StaticResource FontSizeLG}" FontAttributes="Bold"/>

            <CollectionView ItemsSource="{Binding Activities}"
                            EmptyView="Sin actividades asignadas.">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,{StaticResource SpacingXS}" Padding="{StaticResource SpacingSM}"
                                StrokeThickness="1" Stroke="{StaticResource Gray200}"
                                StrokeShape="RoundRectangle {StaticResource RadiusSM}">
                            <VerticalStackLayout Spacing="{StaticResource SpacingXS}">
                                <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="15"/>
                                <Label Text="{Binding Description}" FontSize="{StaticResource FontSizeXS}" TextColor="{StaticResource TextMuted}"/>
                                <Label Text="{Binding PercentageValue, StringFormat='Valor: {0}%'}" FontSize="{StaticResource FontSizeXS}"/>
                                <Label Text="{Binding DueDate, StringFormat='Entrega: {0:dd/MM/yyyy}'}" FontSize="{StaticResource FontSizeXS}" TextColor="{StaticResource TextMuted}"/>
                                <Label Text="{Binding Questions.Count, StringFormat='Preguntas: {0}'}" FontSize="{StaticResource FontSizeXXS}" TextColor="{StaticResource PrimaryDark}"/>
                                <!-- Botones para Profesor -->
                                <HorizontalStackLayout Spacing="{StaticResource SpacingSM}" Margin="0,{StaticResource SpacingMD},0,0"
                                    IsVisible="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.IsTeacher}">
                                    <Button Text="✏ Editar"
                                            FontSize="{StaticResource FontSizeXXS}" Padding="10,6" CornerRadius="{StaticResource RadiusSM}"
                                            BackgroundColor="{StaticResource Primary}" TextColor="White"
                                            CommandParameter="{Binding .}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditActivityCommand}"/>
                                    <Button Text="📊 Resultados"
                                            FontSize="{StaticResource FontSizeXXS}" Padding="10,6" CornerRadius="{StaticResource RadiusSM}"
                                            BackgroundColor="{StaticResource AccentTeal}" TextColor="White"
                                            CommandParameter="{Binding .}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ViewResultsCommand}"/>
                                    <Button Text="🗑 Eliminar"
                                            FontSize="{StaticResource FontSizeXXS}" Padding="10,6" CornerRadius="{StaticResource RadiusSM}"
                                            BackgroundColor="{StaticResource DeleteColor}" TextColor="White"
                                            CommandParameter="{Binding .}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteActivityCommand}"/>
                                </HorizontalStackLayout>
                                <!-- Botón para Estudiante -->
                                <Button Text="📝 Responder actividad"
                                        FontSize="{StaticResource FontSizeXXS}" Padding="10,6" CornerRadius="{StaticResource RadiusSM}"
                                        BackgroundColor="{StaticResource AccentTeal}" TextColor="White"
                                        CommandParameter="{Binding .}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.TakeActivityCommand}"
                                        IsVisible="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.IsStudent}"
                                        HorizontalOptions="Start"
                                        Margin="0,{StaticResource SpacingMD},0,0"/>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Lista estudiantes (solo profesor) -->
            <Label Text="Estudiantes del curso"
                   FontSize="{StaticResource FontSizeLG}" FontAttributes="Bold"
                   IsVisible="{Binding IsTeacher}"/>

            <CollectionView ItemsSource="{Binding Students}"
                            IsVisible="{Binding IsTeacher}"
                            EmptyView="Sin estudiantes.">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,{StaticResource SpacingXS}" Padding="{StaticResource SpacingSM}"
                                StrokeThickness="1" Stroke="{StaticResource Gray200}"
                                StrokeShape="RoundRectangle {StaticResource RadiusSM}">
                            <Grid ColumnDefinitions="*,Auto">
                                <VerticalStackLayout Grid.Column="0">
                                    <Label Text="{Binding Name}" FontAttributes="Bold"/>
                                    <Label Text="{Binding Email}" FontSize="{StaticResource FontSizeXS}" TextColor="{StaticResource TextMuted}"/>
                                </VerticalStackLayout>
                                <Button Grid.Column="1"
                                        Text="Ver perfil"
                                        CommandParameter="{Binding .}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ViewStudentProfileCommand}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
