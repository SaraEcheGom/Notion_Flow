<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NotionFlow.App.Views.Auth.UserProfilePage"
             Title="Profile">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Border BackgroundColor="#2d2d2d" StrokeShape="RoundRectangle 50"
                    HeightRequest="100" WidthRequest="100"
                    HorizontalOptions="Center" Padding="0">
                <Label Text="{Binding Initial}"
                       FontSize="40" FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </Border>

            <Label Text="{Binding Name}"
                   FontSize="22" FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <Label Text="{Binding Role}"
                   FontSize="14" TextColor="Gray"
                   HorizontalOptions="Center" />

            <BoxView HeightRequest="1" Color="LightGray" Margin="0,10" />

            <Label Text="Email" FontSize="13" TextColor="Gray" />
            <Label Text="{Binding Email}" FontSize="16" />

            <Label Text="Cursos asignados" FontSize="13"
                   TextColor="Gray" Margin="0,10,0,0" />

            <CollectionView ItemsSource="{Binding Courses}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,4" Padding="12"
                                StrokeThickness="1" Stroke="LightGray"
                                StrokeShape="RoundRectangle 8">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}"
                                       FontAttributes="Bold" />
                                <Label Text="{Binding Subject}"
                                       FontSize="13" TextColor="Gray" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
