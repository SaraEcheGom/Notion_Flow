<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NotionFlow.App.Views.Teacher.TeacherPage"
             Title="Mis Cursos"
             BackgroundColor="{StaticResource PageBackground}">
    <ScrollView>
        <VerticalStackLayout Spacing="0">
            <Border BackgroundColor="#F4A0C0"
                    StrokeThickness="0" Padding="20,28,20,20">
                <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                    <Label Text="&#128105;&#8205;&#127979;" FontSize="36" HorizontalOptions="Center"/>
                    <Label Text="Mis Cursos" FontSize="22" FontAttributes="Bold"
                           TextColor="White" HorizontalOptions="Center"/>
                    <Label Text="Gestiona tus cursos" FontSize="12"
                           TextColor="White" Opacity="0.9" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Border>
            <VerticalStackLayout Padding="16,16" Spacing="12">
                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                    <Button Grid.Column="0" Text="&#8635; Actualizar"
                            Command="{Binding LoadCoursesCommand}"
                            Style="{StaticResource OutlineBtn}"/>
                    <Button Grid.Column="1" Text="Cerrar sesi&#243;n"
                            Command="{Binding LogoutCommand}"
                            BackgroundColor="#6BAADD"
                            TextColor="White" CornerRadius="16" Padding="0,12"/>
                </Grid>
                <Border BackgroundColor="#e0f8ee" StrokeThickness="0"
                        StrokeShape="RoundRectangle 14" Padding="12,10">
                    <Label Text="{Binding Courses.Count, StringFormat='&#128218; Total de cursos: {0}'}"
                           FontSize="14" FontAttributes="Bold"
                           TextColor="{StaticResource PrimaryDark}"/>
                </Border>
                <VerticalStackLayout x:Name="CoursesContainer" Spacing="10"/>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

